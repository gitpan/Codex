A1 builtin conditional proof
A2 builtin proof by induction
A3 builtin tautology
A4 builtin combine
A5 builtin substitution
M1 maths a * 0 = 0
M2 maths a * (b + 1) = a * b + a
M3 maths a + 0 = a
M4 maths (a = b) = (a + c = b + c)
M5 maths (a + b) + c = a + (b + c)

1 (A3, a * b) a * b = a * b
2 (A5, 1, M3:a=b, -1) a * (b + 0) = a * b
3 (A4, 2, M3:a=a * b) a * (b + 0) = a * b + 0
4 (A5, 3, M1, -2) a * (b + 0) = a * b + a * 0

5 scope a * (b + c) = a * b + a * c [
  6 (A4, 5, M4:a=a * (b + c),b=a * b + a * c,c=a) a * (b + c) + a = (a * b + a * c) + a
  7 (A4, M2:b=b + c, 6) a * ((b + c) + 1) = (a * b + a * c) + a
  8 (A5, 7, M5:a=b,b=c,c=1, 1) a * (b + (c + 1)) = (a * b + a * c) + a
  9 (A5, 8, M5:a=a * b,b=a * c,c=a, 1) a * (b + (c + 1)) = a * b + (a * c + a)
  10 (A5, 9, M2:b=c, -1) a * (b + (c + 1)) = a * b + a * (c + 1)
]
11 (A1, 5, 10) (a * (b + c) = a * b + a * c) => (a * (b + (c + 1)) = a * b + a * (c + 1))
12 (A2, 4, 11) a * (b + c) = a * b + a * c
