A1 builtin conditional proof
A2 builtin proof by induction
A3 builtin tautology
A4 builtin combine
A5 builtin substitution
M1 maths a * 0 = 0
M2 maths a * (b + 1) = a * b + a
M3 maths (a = b) = (a + c = b + c)
M4 maths a * (b + c) = a * b + a * c
M5 maths 0 + a = a

1 (M1:a=a * b) (a * b) * 0 = 0
2 (A5, M1, M1:a=b, -1) a * (b * 0) = 0
3 (A4, 1, 2) (a * b) * 0 = a * (b * 0)

4 scope (a * b) * c = a * (b * c) [
  5 (A4, 4, M3:a=(a * b) * c,b=a * (b * c),c=a * b) (a * b) * c + a * b = a * (b * c) + a * b
  6 (A4, M2:a=a * b,b=c, 5) (a * b) * (c + 1) = a * (b * c) + a * b
  7 (A3, a * (b * (c + 1))) a * (b * (c + 1)) = a * (b * (c + 1))
  8 (A5, 7, M4:a=b,b=c,c=1, 2) a * (b * (c + 1)) = a * (b * c + b * 1)
  9 (A5, 8, M5:a=1, -2) a * (b * (c + 1)) = a * (b * c + b * (0 + 1))
  10 (A5, 9, M2:a=b,b=0, 1) a * (b * (c + 1)) = a * (b * c + (b * 0 + b))
  11 (A5, 10, M1:a=b, 1) a * (b * (c + 1)) = a * (b * c + (0 + b))
  12 (A5, 11, M5:a=b, 1) a * (b * (c + 1)) = a * (b * c + b)
  13 (A4, 12, M4:b=b * c,c=b) a * (b * (c + 1)) = a * (b * c) + a * b
  14 (A4, 6, 13) (a * b) * (c + 1) = a * (b * (c + 1))
]
15 (A1, 4, 14) ((a * b) * c = a * (b * c)) => ((a * b) * (c + 1) = a * (b * (c + 1)))
16 (A2, 3, 15) (a * b) * c = a * (b * c)
